<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Snaptaskv01" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/sapphireBlue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Dark background -->
        <item name="android:colorBackground">@color/charcoalBlack</item>
        <item name="colorSurface">@color/charcoalBlack</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorOnBackground">@color/white</item>

        <item name="android:statusBarColor">@color/sapphireBlue</item>
        <item name="android:navigationBarColor">@color/charcoalBlack</item>
        <item name="android:navigationBarDividerColor">@color/softMintGreen</item>

        <!-- Optional -->
        <item name="bottomNavigationStyle">@style/Widget.Material3.BottomNavigationView</item>
    </style>

</resources>
